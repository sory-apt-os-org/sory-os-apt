name: (disabled) Publish SoryOS APT Repository
on:
  workflow_dispatch:
  push:
    branches: [__disabled__]
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "apt-publish legacy disabled — use build-deb-release + publish-pages"
